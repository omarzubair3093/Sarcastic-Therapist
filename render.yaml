services:
  - type: web
    name: sarcastic-therapist
    env: python
    pythonVersion: "3.11.9" # Provide the full major.minor.patch version
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT app:app"
    envVars:
      # ... your other envVars if any
